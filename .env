# Server configuration
PORT=8080                                # Port the webhook server runs on

# Webhook settings
WEBHOOK_URL=http://localhost:8080/webhooks/yaya   # Local webhook endpoint
WEBHOOK_SECRET=71439365C92E4DEBD34E1F559D6F7      # Shared secret for HMAC verification
TRUSTED_YAYA_IPS=                                # Optional: restrict to trusted IPs
REPLAY_TOLERANCE_SECONDS=300                     # Prevent replays older than 5 mins, before and after
INVOICE_URL=https://yayawallet.com/en/invoice/xxxx  # Example invoice URL for reference

# Rate limiter settings
RATE_LIMIT_WINDOW_MS=60000   # Time window (1 minute)
RATE_LIMIT_MAX=5             # Max 5 requests per IP per window
